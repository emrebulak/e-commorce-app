import { createAction } from '@ngrx/store';

export const increment = createAction('[Basket Component] Increment');
export const decrement = createAction('[Basket Component] Decrement');
export const reset = createAction('[Basket Component] Reset');